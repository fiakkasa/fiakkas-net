@inherits LayoutComponentBase

<NavMenu />

<main class="order-1 flex-fill oveflow-x-hidden overflow-y-auto py-1 px-3">
    <ErrorBoundary @ref="errorBoundary">
        <ChildContent>
            @Body
        </ChildContent>
        <ErrorContent>
            <div class="h-100 d-flex flex-column justify-content-center align-items-center">
                <div class="mb-0 mb-md-5">
                    <i class="bi bi-exclamation-triangle text-warning"></i>
                    <span>Whoops, something went wrong...</span>
                    <a href="" class="btn btn-outline-primary btn-sm rounded-5 p-0 px-1" title="Reload">
                        <i class="bi bi-arrow-clockwise"></i>
                    </a>
                </div>
            </div>
        </ErrorContent>
    </ErrorBoundary>
</main>

<div id="blazor-error-ui" class="order-first order-md-last order-sm-0 flex-shrink-0 py-2 px-3">
    <i class="bi bi-exclamation-triangle text-warning"></i>
    <span>Whoops, something went wrong...</span>
    <a href="" class="btn btn-outline-primary btn-sm rounded-5 p-0 px-1" title="Reload">
        <i class="bi bi-arrow-clockwise"></i>
    </a>
</div>

@code {
    private ErrorBoundary? errorBoundary;

    protected override void OnParametersSet() => errorBoundary?.Recover();
}
